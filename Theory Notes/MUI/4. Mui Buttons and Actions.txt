🔘 Topic 4: MUI Core Components – Buttons & Actions (Complete Guide with Definitions & Code)
-----------------------------------------------------------------------------------------------
Buttons are the primary interaction elements in any application.
They trigger actions like submit, delete, save, navigate, or toggle states.

In Material UI (MUI), button components follow:
Material Design principles
Accessibility standards
Theme consistency
Visual hierarchy rules
State-based styling (hover, focus, disabled, loading)

This guide covers:
Button
IconButton
Fab
ButtonGroup
LoadingButton

1️⃣ Button
-----------
Button is a clickable component used to trigger actions such as submitting a form, navigating, or executing logic.
It is the most commonly used action component.

🔎 Core Concepts
----------------
a. Variants (Visual Hierarchy)
----------------------------
variant="text"
variant="outlined"
variant="contained"

text → lowest emphasis
outlined → medium emphasis
contained → highest emphasis

Material Design principle:
---------------------------
> Important actions should stand out visually.

b. Colors
----------
color="primary"
color="secondary"
color="error"

Uses theme palette.

c. Sizes
---------
size="small"
size="medium"
size="large"

d. Disabled State
-----------------
disabled
Prevents user interaction.

💻 Example
-------------
import { Button } from "@mui/material";

function Example() {
  return (
    <Button
      variant="contained"
      color="primary"
      size="large"
      onClick={() => alert("Submitted")}
    >
      Submit
    </Button>
  );
}

🎯 When to Use
✔ Form submission
✔ Navigation
✔ Primary actions

2️⃣ IconButton
--------------
IconButton is a button that contains only an icon, used for compact actions.
It is circular and optimized for toolbars or action lists.

🔎 Core Concepts
---------------
Small footprint
Used with icons (e.g., delete, edit)
Supports ripple effect
Works well with Tooltip

💻 Example
------------
import { IconButton } from "@mui/material";
import DeleteIcon from "@mui/icons-material/Delete";

function Example() {
  return (
    <IconButton color="error">
      <DeleteIcon />
    </IconButton>
  );
}

🎯 When to Use
✔ Table row actions
✔ Toolbar actions
✔ Compact UI controls

3️⃣ Fab (Floating Action Button)
--------------------------------
Fab (Floating Action Button) is a circular button used for a primary screen-level action.
It “floats” above content.

🔎 Core Concepts
Represents most important action
Typically placed bottom-right
Circular design
Uses elevation (shadow)

💻 Example
-----------
import { Fab } from "@mui/material";
import AddIcon from "@mui/icons-material/Add";

function Example() {
  return (
    <Fab color="primary">
      <AddIcon />
    </Fab>
  );
}

🎯 When to Use
✔ Add new item
✔ Create action
✔ Chat open button

Avoid: ❌ Multiple FABs on same screen
❌ Secondary actions

4️⃣ ButtonGroup
----------------
ButtonGroup groups multiple buttons together to create a unified control.
Used when actions are related.

🔎 Core Concepts
Horizontal or vertical layout
Shared variant & color
Logical grouping

💻 Example
------------
import { Button, ButtonGroup } from "@mui/material";

function Example() {
  return (
    <ButtonGroup variant="outlined">
      <Button>Left</Button>
      <Button>Center</Button>
      <Button>Right</Button>
    </ButtonGroup>
  );
}

🎯 When to Use

✔ Pagination controls
✔ Text formatting (bold/italic)
✔ Toggle actions

5️⃣ LoadingButton
-----------------
(From MUI Lab package)
LoadingButton is an enhanced Button that shows a loading spinner while an async action is in progress.

Prevents duplicate submissions.

🔎 Core Concepts
loading prop controls spinner
Prevents multiple clicks
Shows progress feedback

💻 Example
-----------
import { LoadingButton } from "@mui/lab";
import React from "react";

function Example() {
  const [loading, setLoading] = React.useState(false);

  const handleClick = () => {
    setLoading(true);
    setTimeout(() => setLoading(false), 2000);
  };

  return (
    <LoadingButton
      loading={loading}
      variant="contained"
      onClick={handleClick}
    >
      Save
    </LoadingButton>
  );
}

🎯 When to Use
✔ API calls
✔ Form submissions
✔ Login button
✔ Payment processing


🧠 Visual Hierarchy Concept (Very Important)
--------------------------------------
Not all buttons should look the same.

Correct hierarchy:
1. Primary → contained
2. Secondary → outlined
3. Tertiary → text

Example:
----------
<Button variant="text">Cancel</Button>
<Button variant="contained">Save</Button>

🏗 Accessibility Concepts
-------------------------
All buttons:
Support keyboard navigation
Have focus states
Should include descriptive text
IconButton should use aria-label

Example:
<IconButton aria-label="delete">

🎯 Enterprise Usage Strategy
Component	Use Case
--------------------
Button	Standard actions
IconButton	Compact actions
Fab	Primary screen action
ButtonGroup	Grouped related actions
LoadingButton	Async operations

🚀 Best Practices
-------------------
✅ Use LoadingButton for API calls
✅ Maintain visual hierarchy
✅ Don’t overuse FAB
✅ Use aria-label for icon buttons
✅ Disable button during processing


Final Thought
-----------------
Buttons represent intent and priority in UI design.
Correct usage improves:
UX clarity
Accessibility
Visual consistency