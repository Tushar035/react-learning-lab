🚀 Topic 9 : MUI X – Advanced Components Guide
----------------------------------------------
When building enterprise dashboards, admin panels, analytics tools, the core components of Material UI are not always enough.

That’s where MUI X comes in.
MUI X provides:
Advanced data grids
Date & time pickers
Charts
Tree View
Premium enterprise component

📊 1️⃣ DataGrid (Community vs Pro)
-------------------------------------
DataGrid is a powerful table component designed for handling large structured datasets with built-in features like sorting, filtering, pagination, and virtualization.

It is optimized for performance.

📦 Packages
------------
Community (Free)
----------------
npm install @mui/x-data-grid

Pro (Paid)
----------
npm install @mui/x-data-grid-pro

🆚 Community vs Pro
------------------------
Feature	Community	Pro

Sorting       	✅	✅
Filtering	      ✅	✅
Pagination	    ✅	✅
Row grouping	  ❌	✅
Column pinning	❌	✅
Excel export	  ❌	✅
Aggregation	    ❌	✅

💻 Basic Example (Community)
-----------------------------
import { DataGrid } from "@mui/x-data-grid";

const rows = [
  { id: 1, name: "Tushar", age: 25 },
  { id: 2, name: "Rahul", age: 28 },
];

const columns = [
  { field: "id", headerName: "ID", width: 90 },
  { field: "name", headerName: "Name", width: 150 },
  { field: "age", headerName: "Age", width: 110 },
];

function Example() {
  return (
    <div style={{ height: 400 }}>
      <DataGrid rows={rows} columns={columns} pageSize={5} />
    </div>
  );
}

🧠 Enterprise Use Cases
-----------------------
✔ Admin panels
✔ CRM dashboards
✔ Financial reports
✔ Analytics systems

📅 2️⃣ Date Pickers
--------------------
DatePicker allows users to select a date via calendar UI.

Requires installation:

npm install @mui/x-date-pickers

💻 Example
-------------
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { LocalizationProvider } from "@mui/x-date-pickers";
import { AdapterDayjs } from "@mui/x-date-pickers/AdapterDayjs";
import dayjs from "dayjs";
import { TextField } from "@mui/material";
import React from "react";

function Example() {
  const [value, setValue] = React.useState(dayjs());

  return (
    <LocalizationProvider dateAdapter={AdapterDayjs}>
      <DatePicker
        label="Select Date"
        value={value}
        onChange={(newValue) => setValue(newValue)}
        renderInput={(params) => <TextField {...params} />}
      />
    </LocalizationProvider>
  );
}

🔎 Key Concept
---------------
LocalizationProvider is required for date formatting.

⏰ 3️⃣ Time Pickers
--------------------
TimePicker allows users to select time via clock interface.

💻 Example
------------
import { TimePicker } from "@mui/x-date-pickers/TimePicker";

function Example() {
  const [value, setValue] = React.useState(dayjs());

  return (
    <LocalizationProvider dateAdapter={AdapterDayjs}>
      <TimePicker
        label="Select Time"
        value={value}
        onChange={(newValue) => setValue(newValue)}
        renderInput={(params) => <TextField {...params} />}
      />
    </LocalizationProvider>
  );
}

🎯 Use Cases
-----------
✔ Booking systems
✔ Appointment scheduling
✔ Event management

📈 4️⃣ Charts
---------------
MUI X provides modern chart components like:
BarChart
LineChart
PieChart

Install:
-------
npm install @mui/x-charts

💻 Example
----------
import { BarChart } from "@mui/x-charts/BarChart";

function Example() {
  return (
    <BarChart
      xAxis={[{ scaleType: "band", data: ["Jan", "Feb", "Mar"] }]}
      series={[{ data: [30, 50, 70] }]}
      width={500}
      height={300}
    />
  );
}

🧠 Enterprise Usage
---------------------
✔ KPI dashboards
✔ Revenue charts
✔ User analytics
✔ Financial visualization

🌳 5️⃣ Tree View
------------------
TreeView displays hierarchical data in expandable/collapsible nodes.

💻 Example
--------------
import { TreeView, TreeItem } from "@mui/x-tree-view";
import React from "react";

function Example() {
  return (
    <TreeView>
      <TreeItem nodeId="1" label="Parent">
        <TreeItem nodeId="2" label="Child 1" />
        <TreeItem nodeId="3" label="Child 2" />
      </TreeItem>
    </TreeView>
  );
}

🎯 Use Cases
------------
✔ File explorer
✔ Folder structure
✔ Category hierarchy
✔ Role permissions

🧠 Architecture-Level Insight
------------------------------
Component	Best For
DataGrid	Large datasets
DatePicker	Date selection
TimePicker	Time input
Charts	Analytics
TreeView	Hierarchical data

🏗 Enterprise Strategy
----------------------
If building scalable admin systems:
Use DataGrid for structured data
Use Pickers for filters
Use Charts for reporting
Use TreeView for nested categories

🚀 Final Thought
-------------------
MUI X turns your app into:
✔ Enterprise-ready
✔ Analytics-capable
✔ Data-heavy dashboard
✔ Professional UI