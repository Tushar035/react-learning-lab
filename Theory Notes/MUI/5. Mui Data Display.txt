📑 Topic 5: MUI Core Components – Data Display (Definitions + Concepts + Code)
------------------------------------------------------------------------------
Displaying data clearly is just as important as collecting it.
Typography, tables, cards, lists — these components determine readability, hierarchy, and usability.

In Material UI (MUI), data display components follow:
Material Design typography scale
Theme-based spacing
Accessible UI patterns
Responsive behavior
Visual hierarchy principles

This guide covers:
Typography
Avatar
Badge
Chip
Tooltip
Card
Table
List

1️⃣ Typography
-------------
Typography is a component used to display text with predefined styles based on Material Design type scale.

It ensures consistent font size, weight, and spacing across the app.

🔎 Core Concepts
--------------
1. Variants
------------
variant="h1"
variant="h2"
variant="body1"
variant="caption"

Common variants:
----------------
h1–h6 → headings
subtitle1 / subtitle2
body1 / body2
caption
button

2. Semantic vs Visual Element
--------------------------------
<Typography variant="h4" component="h1">
variant → visual style
component → actual HTML tag

💻 Example
------------
import { Typography } from "@mui/material";

function Example() {
  return (
    <>
      <Typography variant="h4">
        Dashboard
      </Typography>

      <Typography variant="body1">
        Welcome to your analytics overview.
      </Typography>
    </>
  );
}

🎯 When to Use
--------------
✔ Page titles
✔ Section headers
✔ Body content
✔ Captions

2️⃣ Avatar
-----------
Avatar displays a user's profile image, initials, or icon in a circular container.

🔎 Core Concepts
------------------
Can show image
Can show initials
Can show icon
Supports sizing via sx

💻 Example
--------------
import { Avatar } from "@mui/material";

function Example() {
  return (
    <Avatar alt="Tushar Patil">
      TP
    </Avatar>
  );
}

🎯 When to Use
----------------
✔ User profiles
✔ Comments
✔ Team members
✔ Chat lists

3️⃣ Badge
-----------
Badge displays a small status indicator or notification count attached to another component.

🔎 Core Concepts
------------
badgeContent → number/value
color → theme color
variant="dot" → small indicator

💻 Example
-----------
import { Badge, IconButton } from "@mui/material";
import MailIcon from "@mui/icons-material/Mail";

function Example() {
  return (
    <IconButton>
      <Badge badgeContent={4} color="error">
        <MailIcon />
      </Badge>
    </IconButton>
  );
}

🎯 When to Use
-----------------
✔ Notification count
✔ Message alerts
✔ Status indicators

4️⃣ Chip
----------
Chip is a compact element used to represent small pieces of information such as tags, filters, or selections.

🔎 Core Concepts
----------------
label → displayed text
onDelete → shows delete icon
clickable behavior
Can include icon or avatar

💻 Example
-----------
import { Chip } from "@mui/material";

function Example() {
  return (
    <Chip
      label="React"
      onDelete={() => alert("Deleted")}
      color="primary"
    />
  );
}

🎯 When to Use
--------------
✔ Tags
✔ Filter selections
✔ Category labels
✔ Status indicators

5️⃣ Tooltip
-------------
Tooltip displays a short informative message when users hover, focus, or tap on an element.

🔎 Core Concepts
----------------
Appears on hover/focus
title prop defines text
placement controls position

💻 Example
-----------
import { Tooltip, IconButton } from "@mui/material";
import DeleteIcon from "@mui/icons-material/Delete";

function Example() {
  return (
    <Tooltip title="Delete item" placement="top">
      <IconButton>
        <DeleteIcon />
      </IconButton>
    </Tooltip>
  );
}

🎯 When to Use
--------------
✔ Icon-only buttons
✔ Short explanations
✔ Additional context

6️⃣ Card
--------
Card is a surface component used to group related content and actions.

It is built on top of Paper.

🔎 Core Concepts
-----------------
Card structure:
Card
CardContent
CardActions
CardMedia

💻 Example
--------------
import { Card, CardContent, CardActions, Typography, Button } from "@mui/material";

function Example() {
  return (
    <Card sx={{ maxWidth: 300 }}>
      <CardContent>
        <Typography variant="h6">
          React Course
        </Typography>
        <Typography variant="body2">
          Learn modern React step by step.
        </Typography>
      </CardContent>
      <CardActions>
        <Button size="small">Enroll</Button>
      </CardActions>
    </Card>
  );
}

🎯 When to Use
--------------
✔ Dashboard widgets
✔ Product previews
✔ Course previews
✔ Content blocks

7️⃣ Table
----------
Table displays structured data in rows and columns.

🔎 Core Concepts
-----------------
Table structure:
Table
TableHead
TableRow
TableCell
TableBody

💻 Example
------------
import {
  Table, TableHead, TableRow,
  TableCell, TableBody
} from "@mui/material";

function Example() {
  return (
    <Table>
      <TableHead>
        <TableRow>
          <TableCell>Name</TableCell>
          <TableCell>Age</TableCell>
        </TableRow>
      </TableHead>
      <TableBody>
        <TableRow>
          <TableCell>Tushar</TableCell>
          <TableCell>25</TableCell>
        </TableRow>
      </TableBody>
    </Table>
  );
}

🎯 When to Use
-----------------
✔ Admin dashboards
✔ Reports
✔ Data management panels

8️⃣ List
--------
List is used to display vertical collections of related items.

🔎 Core Concepts
----------------
List
ListItem
ListItemText
ListItemIcon

💻 Example
----------
import {
  List, ListItem,
  ListItemText, ListItemIcon
} from "@mui/material";
import InboxIcon from "@mui/icons-material/Inbox";

function Example() {
  return (
    <List>
      <ListItem>
        <ListItemIcon>
          <InboxIcon />
        </ListItemIcon>
        <ListItemText primary="Inbox" />
      </ListItem>
    </List>
  );
}

🎯 When to Use
---------------
✔ Navigation menus
✔ Sidebar
✔ Chat list
✔ Settings page

🧠 Visual Hierarchy Understanding
--------------------------------
Correct UI layering:
1. Typography → structure text
2. Card → group related content
3. List → organize vertical items
4. Table → structured data
5. Badge → highlight attention
6. Tooltip → add context

🚀 Enterprise Strategy
----------------------
For scalable apps:
Use Typography consistently
Use Cards for modular UI
Use Tables for data-heavy screens
Use Lists for navigation
Avoid mixing too many visual patterns