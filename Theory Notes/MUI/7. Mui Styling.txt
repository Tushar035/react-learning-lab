 Topic 7 : Styling in MUI 

Styling in Material UI (MUI) is one of its strongest features.

Unlike traditional CSS approaches, MUI provides:

Theme-driven styling

Component-scoped styling

Responsive utilities

Dark mode support

Design system consistency


If you're building scalable dashboards or enterprise apps, mastering this section is critical.


---

1️⃣ sx Prop

✅ Definition

The sx prop is a powerful inline styling utility that allows you to apply theme-aware CSS styles directly to MUI components.

It supports:

Theme values

Spacing system

Breakpoints

Pseudo-selectors

Nested styling



---

🔎 Core Concepts

1. Theme-Aware Styling

<Box sx={{ bgcolor: "primary.main" }} />

Uses theme palette instead of hardcoded colors.


---

2. Spacing Scale (8px system)

<Box sx={{ p: 2 }} />

2 × 8px = 16px


---

3. Responsive Styling

<Box sx={{ width: { xs: "100%", md: "50%" } }} />

Mobile → 100%
Desktop → 50%


---

💻 Example

import { Box } from "@mui/material";

function Example() {
  return (
    <Box
      sx={{
        p: 3,
        bgcolor: "secondary.main",
        color: "white",
        borderRadius: 2,
        "&:hover": {
          bgcolor: "secondary.dark"
        }
      }}
    >
      Styled with sx
    </Box>
  );
}


---

🎯 When to Use

✔ Quick styling
✔ Component-level customization
✔ Responsive adjustments


---

2️⃣ styled() API

✅ Definition

The styled() API is a utility for creating custom styled components with reusable styles.

It is ideal for larger reusable UI components.


---

🔎 Concept

It wraps a component and applies CSS with theme access.


---

💻 Example

import { styled } from "@mui/material/styles";
import { Button } from "@mui/material";

const CustomButton = styled(Button)(({ theme }) => ({
  backgroundColor: theme.palette.primary.main,
  padding: theme.spacing(2),
  borderRadius: 12,
  "&:hover": {
    backgroundColor: theme.palette.primary.dark,
  },
}));

function Example() {
  return <CustomButton>Styled Button</CustomButton>;
}


---

🎯 When to Use

✔ Reusable design system components
✔ Large applications
✔ Shared UI library


---

3️⃣ Theme Customization

✅ Definition

Theme customization allows you to override default MUI design tokens such as colors, typography, spacing, and breakpoints.

This creates a consistent design system.


---

💻 Basic Theme Setup

import { createTheme, ThemeProvider } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2",
    },
  },
});

function App() {
  return (
    <ThemeProvider theme={theme}>
      <YourApp />
    </ThemeProvider>
  );
}


---

4️⃣ Custom Palette

✅ Definition

Custom palette defines brand colors used across the application.


---

💻 Example

const theme = createTheme({
  palette: {
    primary: {
      main: "#0057D9",
      light: "#4D8BFF",
      dark: "#003C9E",
    },
    success: {
      main: "#2E7D32",
    },
  },
});


---

🔎 Why Important?

Brand consistency

Avoid hardcoded colors

Centralized color management



---

5️⃣ Custom Typography

✅ Definition

Typography customization modifies font families, sizes, weights, and heading styles globally.


---

💻 Example

const theme = createTheme({
  typography: {
    fontFamily: "Poppins, sans-serif",
    h1: {
      fontSize: "2.5rem",
      fontWeight: 700,
    },
    body1: {
      fontSize: "1rem",
    },
  },
});


---

🎯 Benefits

✔ Consistent text scale
✔ Professional appearance
✔ Design system alignment


---

6️⃣ Global Styles

✅ Definition

Global styles apply CSS rules across the entire application.


---

💻 Example

import { GlobalStyles } from "@mui/material";

function Example() {
  return (
    <>
      <GlobalStyles
        styles={{
          body: {
            margin: 0,
            padding: 0,
          },
        }}
      />
    </>
  );
}


---

🎯 When to Use

✔ Reset styles
✔ Global font settings
✔ Base layout rules


---

7️⃣ Breakpoints

✅ Definition

Breakpoints define screen size thresholds used for responsive design.


---

Default Breakpoints

Key	Width

xs	0px
sm	600px
md	900px
lg	1200px
xl	1536px



---

💻 Example

<Box
  sx={{
    fontSize: {
      xs: "14px",
      md: "18px",
    },
  }}
>
  Responsive Text
</Box>


---

Custom Breakpoints

const theme = createTheme({
  breakpoints: {
    values: {
      xs: 0,
      sm: 500,
      md: 800,
      lg: 1100,
      xl: 1400,
    },
  },
});


---

8️⃣ Responsive Design

✅ Definition

Responsive design ensures the UI adapts to different screen sizes.


---

Techniques in MUI

1. Breakpoint-based styling


2. Grid system


3. useMediaQuery hook




---

💻 Example using useMediaQuery

import { useMediaQuery } from "@mui/material";
import { useTheme } from "@mui/material/styles";

function Example() {
  const theme = useTheme();
  const isMobile = useMediaQuery(theme.breakpoints.down("sm"));

  return <div>{isMobile ? "Mobile View" : "Desktop View"}</div>;
}


---

9️⃣ Dark Mode Implementation

✅ Definition

Dark mode switches the application color scheme to a darker palette for better night usability and reduced eye strain.


---

💻 Basic Dark Mode Theme

const darkTheme = createTheme({
  palette: {
    mode: "dark",
  },
});


---

💻 Toggle Example

import React from "react";
import { createTheme, ThemeProvider } from "@mui/material/styles";
import { Switch } from "@mui/material";

function App() {
  const [darkMode, setDarkMode] = React.useState(false);

  const theme = createTheme({
    palette: {
      mode: darkMode ? "dark" : "light",
    },
  });

  return (
    <ThemeProvider theme={theme}>
      <Switch
        checked={darkMode}
        onChange={() => setDarkMode(!darkMode)}
      />
      <div style={{ padding: 20 }}>Toggle Dark Mode</div>
    </ThemeProvider>
  );
}


---

🧠 Architecture-Level Understanding

In scalable apps:

Use sx for small styling tweaks

Use styled() for reusable components

Use createTheme() for global design system

Avoid hardcoded values

Keep all design tokens centralized



---

🚀 Enterprise Best Practices

✔ Define theme in one place
✔ Use theme palette everywhere
✔ Avoid inline CSS
✔ Implement dark mode from day one
✔ Use breakpoints consistently


---

🎯 Final Thought

Styling in MUI is not just about appearance —
It is about building a design system-driven architecture.

Master this and your UI becomes:

✔ Scalable
✔ Consistent
✔ Maintainable
✔ Enterprise-ready


